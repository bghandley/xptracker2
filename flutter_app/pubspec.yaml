name: heroic_everyday
description: "Heroic Everyday tracker (Flutter). Professionally playful UX with habits, missions, XP, and AI helpers."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.0.0
  google_fonts: ^6.1.0
  firebase_core: ^2.23.0
  cloud_firestore: ^4.13.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
