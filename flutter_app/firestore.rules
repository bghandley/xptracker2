rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Require a signed-in Firebase Auth user for all reads/writes.
      allow read, write: if request.auth != null;
    }
  }
}
